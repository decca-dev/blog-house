<%- include('partials/navbar') %> 
<div class="container">
    <h1 class="mt-4 mb-4">Welcome back <%= user.name %></h1>
    <h4>Account created at: <%= moment.utc(user.registeredAt).format("dddd, MMMM Do YYYY") %></h4>
</div>